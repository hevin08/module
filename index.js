const localModule = require('./module');

let ans = localModule.sum(10,2);

let min = localModule.sub(10,2);

let data = localModule.todoList;

console.log(`Sum = ${ans}`);
console.log(`Min = ${min}`);
console.log(data);
